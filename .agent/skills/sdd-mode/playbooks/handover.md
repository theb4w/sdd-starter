# Playbook: handover

**Family:** Session
**Intent:** Going away / stop
**Profile:** n/a
**Basis:** persistent Markdown (spec-kit). Session memory is not the SSOT; `SDD/handovers/` is.
**Template:** `templates/handover.md`

## Steps

1. **Step 0** of `sdd-mode` (ensure `SDD/`).
2. Write `SDD/handovers/handover_<MODULO>[_FASE_X]_<YYYY-MM-DD>.md` from the template. Every section filled (`N/A` + reason if needed).
3. Update `SDD/INDEX.md`.
4. Do not commit unless the human asks.

## Reply

Handover path, INDEX delta, next playbook.
