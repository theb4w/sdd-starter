# Playbook: onboarding

**Family:** Arrive
**Intent:** First chat, `SDD/` already there
**Profile:** `observe`
**Not when:** no `SDD/` and no product docs → `discover.md`. Empty product intent → `bootstrap.md`.
**Basis:** constitution + project context before work. No code this turn.

## Steps

1. **Step 0** of `sdd-mode`. If `SDD/` was just created empty, this is not onboarding — use `discover` or `bootstrap`.
2. Read `SDD/AGENTS.md`, `SDD/INDEX.md`, `SDD/architecture.md` if any, latest handover, this skill (resolve skill root).
3. Answer before any code: absolute rules; pinned versions; modules/status; ADRs that affect today; which catalog row today needs; **`Smoke:`** line in `SDD/AGENTS.md` (missing → next playbook must fill it before any accept).
4. Wait.

## Reply

The six answers, recommended next playbook.
